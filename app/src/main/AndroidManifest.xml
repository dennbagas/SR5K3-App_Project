<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bagslabs.SR5K3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddAccidentsActivity"
            android:label="Add Accidents Data"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddCrashActivity"
            android:label="Add Crash Data"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewAccidentsList"
            android:label="View Accidents Data"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewCrashList"
            android:label="View Crash Data"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AccidentsDetailActivity">
        </activity>
        <activity
            android:name=".CrashDetailActivity">
        </activity>
    </application>

</manifest>
